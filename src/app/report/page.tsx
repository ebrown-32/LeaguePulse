import ReportView from './ReportView';

export const metadata = {
  title: 'Weekly Report',
  description: 'Coaching efficiency, optimal lineups, all-play records and points by position.',
};

export default function ReportPage() {
  return <ReportView />;
}
